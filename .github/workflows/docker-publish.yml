name: Publish Docker image

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Extract version from Cargo.toml
        id: get_version
        run: |
          VERSION=$(grep '^version' src/wg-quickrs/Cargo.toml | head -1 | awk -F'"' '{print $2}')
          echo "version_tag=v${VERSION}" >> $GITHUB_OUTPUT
          echo "Version tag: v${VERSION}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Check if pre-release
        id: check_prerelease
        run: |
          if [[ "${{ steps.set_version.outputs.version_tag }}" == *"-"* ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
          fi

      - name: Build and Push (Multi-arch)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64/v8,linux/arm/v7
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }}:${{ steps.set_version.outputs.version_tag }}
            ${{ steps.check_prerelease.outputs.is_prerelease == 'false' && format('{0}/{1}:latest', secrets.DOCKERHUB_USERNAME, github.event.repository.name) || '' }}
