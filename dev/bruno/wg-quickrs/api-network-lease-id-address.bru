meta {
  name: /api/network/lease/id-address
  type: http
  seq: 5
}

get {
  url: {{base-url}}/api/network/lease/id-address
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
}
