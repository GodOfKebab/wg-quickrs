meta {
  name: /api/wireguard/server/status
  type: http
  seq: 9
}

post {
  url: {{base-url}}/api/wireguard/server/status
  body: json
  auth: inherit
}

body:json {
  {
      "status": 2
  }
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
}
