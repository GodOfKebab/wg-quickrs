meta {
  name: /api/network/config
  type: http
  seq: 8
}

patch {
  url: {{base-url}}/api/network/config
  body: json
  auth: inherit
}

body:json {
  {
      "changed_fields": {
          "peers": {
              "{{thisPeerId}}": {
                  "name": "bruno-test"
              }
          }
      }
  }
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
