meta {
  name: /api/wireguard/public_private_keys
  type: http
  seq: 8
}

get {
  url: {{base-url}}/api/wireguard/public_private_keys
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
}
