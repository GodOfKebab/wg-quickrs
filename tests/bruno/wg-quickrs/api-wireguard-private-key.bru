meta {
  name: /api/wireguard/private-key
  type: http
  seq: 4
}

get {
  url: {{base-url}}/api/wireguard/private-key
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
