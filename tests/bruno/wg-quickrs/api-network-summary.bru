meta {
  name: /api/network/summary
  type: http
  seq: 6
}

get {
  url: {{base-url}}/api/network/summary?only_digest=false
  body: none
  auth: inherit
}

params:query {
  only_digest: false
}

assert {
  res.status: eq 200
  res.body: isJson
}

script:post-response {
  bru.setVar("thisPeerId", res.body.network.this_peer);
}

settings {
  encodeUrl: true
  timeout: 0
}
