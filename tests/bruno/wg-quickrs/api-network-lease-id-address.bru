meta {
  name: /api/network/lease/address
  type: http
  seq: 7
}

get {
  url: {{base-url}}/api/network/lease/address
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
