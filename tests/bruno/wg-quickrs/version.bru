meta {
  name: /version
  type: http
  seq: 3
}

get {
  url: {{base-url}}/version
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
