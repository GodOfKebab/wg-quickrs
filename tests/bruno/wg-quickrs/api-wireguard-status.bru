meta {
  name: /api/wireguard/status
  type: http
  seq: 9
}

post {
  url: {{base-url}}/api/wireguard/status
  body: json
  auth: inherit
}

body:json {
  {
      "status": 1
  }
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
